{
  "name": "quicknote-log",
  "version": "2.0.1",
  "description": "Ứng dụng quản lý lượt khách hàng - offline PWA",
  "scripts": {
    "dev": "npx serve src -l 3000",
    "build": "node build.js",
    "build:watch": "node build.js --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "esbuild": "^0.28.0",
    "html-minifier-terser": "^7.2.0"
  }
}